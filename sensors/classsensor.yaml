---
  enabled: true
  class_name: "ClassSensor"
  entry_point: "swaggerserver/classsensor.py"
  description: "Sensor for training"
  trigger_types:
    -
      name: receivedcall
      description: "Trigger fires contents of call being made so we can create rules."
      payload_schema:
        type: object
        properties:
          operation:
            description: "get, set, delete"
            type: string
          vlan:
            description: "VLAN number"
            type: string
          description:
            description: "VLAN information"
            type: string










