{
  "name": "vroutergetinterfacelist",
  "runner_type": "mistral-v2",
  "tags": [],
  "enabled": true,
  "description": "This end point is a workflow that gets vrouter interface information and marshalls it into JSON then throws it at Slack.",
  "pack": "classpack",
  "entry_point": "workflows/vroutergetinterfacelist.yaml",
  "notify": {},
  "parameters": {
    "skip_notify": {
      "default": [
        "postgetints",
        "getgetints",
        "parseoutput",
        "postresult",
        ""
      ],
      "type": "array",
      "description": "List of tasks to skip notifications for."
    },
    "hostname": {
      "required": true,
      "type": "string"
    },
    "username": {
      "required": true,
      "type": "string",
    },
    "password": {
      "required": true,
      "type": "string",
    }
  },
  "ref": "classpack.vRouterGetInterfaceList",
}

