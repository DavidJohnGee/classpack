---
name: "whoami"
description: "Simple response for giving the audience information about me"
action_ref: "core.noop"
formats:
  - "who am I"
ack:
  enabled: false
result:
  format: "You are {{'David' if execution.context.api_user == 'ipengineer'}} {{execution.context.api_user if execution.context.api_user != 'ipengineer'}}"
  extra:
    slack:
      image_url: "{{'http://ipengineer.net/wp-content/uploads/2014/11/favicon.png' if execution.context.api_user == 'ipengineer' else ''}}"
      fields:
        - title: "{{'Blog' if execution.context.api_user == 'ipengineer' else ''}}"
          value: "{{'http://ipengineer.net / http://davidg.ee' if execution.context.api_user == 'ipengineer' else ''}}"
          short: "{{true if execution.context.api_user == 'ipengineer' else ''}}"
        - title: "{{'Social Media' if execution.context.api_user == 'ipengineer' else ''}}"
          value: "{{'@VTEP42 on Twitter' if execution.context.api_user == 'ipengineer' else ''}}"
          short: "{{true if execution.context.api_user == 'ipengineer' else ''}}"
        - title: "{{'GitHub' if execution.context.api_user == 'ipengineer' else ''}}"
          value: "{{'http://github.com/davidjohngee' if execution.context.api_user == 'ipengineer' else ''}}"
          short: "{{true if execution.context.api_user == 'ipengineer' else ''}}"
      color: "#00AA00"
